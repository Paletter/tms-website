define(function() {
    return ['$scope',"BaseHttp","AppConfig",'$timeout',function($scope,BaseHttp,AppConfig, $timeout){

        var initialize = function () {
        };

        initialize();
    }];
});